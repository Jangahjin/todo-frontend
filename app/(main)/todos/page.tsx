/**
 * 임시 placeholder — 실제 Todo 목록 화면은 Task 029에서 구현한다.
 * (main)/layout.tsx의 인증 가드가 실제로 라우팅에 걸리려면 이 그룹 아래
 * 매치되는 페이지가 최소 하나 필요해 Task 026 범위에서 최소 스텁만 둔다.
 */
export default function TodosPage() {
	return (
		<div className="p-8">
			<p className="text-muted-foreground">Todo 목록 화면은 Task 029에서 구현합니다.</p>
		</div>
	);
}
